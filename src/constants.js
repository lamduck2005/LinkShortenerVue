// src/constants.js

// Giới hạn Validation (Khớp với Backend)

export const URL_MAX_LENGTH = 2048;

export const TEXT_MAX_LENGTH = 50000; // Ví dụ: 50k ký tự

// Giới hạn UI (Chỉ cho Frontend xem trước)

export const TEXT_PREVIEW_LIMIT = 300; // Xem trước 300 ký tự
